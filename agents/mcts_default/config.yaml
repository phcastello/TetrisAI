# Configuracao para o MCTS com rollouts aleatorios (baseline default).
seed: 1337
iterations: 200
rollout_depth: 10
uct_c: 1.4142
# Novos campos configuraveis no agente unificado.
rollout_policy: random
reward_mode: score
use_transposition_table: false
tt_max_entries: 0
# Opcional: defina >0 para encerrar partidas longas automaticamente.
score_limit: 0
time_limit_seconds: 300
